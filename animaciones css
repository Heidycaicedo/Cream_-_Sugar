@keyframes cambioColores {
  0%   { color: #ebad8d; }   /* café claro */
  50%  { color: #ffffff; }   /* blanco */
  100% { color: #1e90ff; }   /* azul */
}

/* Ahora coincide con index.html */
.servicios-1 h3 {
  animation: cambioColores 4s infinite alternate;
}